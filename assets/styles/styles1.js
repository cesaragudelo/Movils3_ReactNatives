import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: 'green',
      alignItems: 'center',
      justifyContent: 'center',
    },
    borders:{
      borderWidth:3,
      borderColor:"black"
    }
  
  });
  
  const mytexts = StyleSheet.create({
    text:{
      color:'red'
    }
  })

  export{styles,mytexts}
  